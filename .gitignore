node_modules/
.next/
out/
dist/
build/
# Root-only: the design mockups folder. Anchored with a leading slash so it
# does NOT also ignore source dirs named "design" (e.g. lib/design/tokens.ts,
# which broke the Vercel/Linux build when it was excluded from the commit).
/design/

# env
.env
.env.local
.env*.local
.claude/settings.json
.claude/**/settings.local.json

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# editor
.vscode/
.idea/
.DS_Store
